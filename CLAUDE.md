fruitcap is a small command-line tool written in Python used to capture and record using AVFoundation to H264 using the Apple Hardware Encoder.
To begin with, it should identify the first capture device, support a programmable resolution setting, and support a programmable bitrate setting.
It should use a windows.ini style config file (fruitcap.cfg).  A very simple command line UI should be supported (q to quit).
